# Andromeda Core Cryptography Library

This repository contains the `corecrypto` kernel-mode cryptography
library, as used in Andromeda. It also contains `libcorecrypto`,
the user-mode equivalent thereof.
